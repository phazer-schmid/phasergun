{
  "name": "@phasergun/chunker",
  "version": "0.2.0",
  "description": "Intelligent document chunking service for RAG systems",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "test": "tsc && node dist/cli.js",
    "dev": "tsc && node dist/cli.js"
  },
  "dependencies": {
    "@phasergun/shared-types": "file:../shared-types",
    "gpt-tokenizer": "^2.1.1"
  },
  "devDependencies": {
    "typescript": "~5.9.0",
    "@types/node": "^20.0.0"
  }
}
