# API Server Environment Variables

# Server Configuration
PORT=3001

# LLM Configuration
LLM_MODE=anthropic
# Options: anthropic, mistral, ollama, mock

# Anthropic API Configuration
ANTHROPIC_API_KEY=your_anthropic_api_key_here
ANTHROPIC_MODEL=claude-3-haiku-20240307
# Options: claude-3-haiku-20240307, claude-3-5-sonnet-20241022

# Mistral API Configuration (optional)
MISTRAL_API_KEY=your_mistral_api_key_here
MISTRAL_MODEL=mistral-small-latest

# Ollama Configuration (optional)
OLLAMA_MODEL=llama3.1:70b
OLLAMA_BASE_URL=http://localhost:11434

# RAG Checks Configuration
# Base path to RAG checks folder (system will automatically append /Phase 1/, /Phase 2/, etc.)
# Example: RAG_CHECKS=/Users/yourusername/RAG
# The system will look for checks in: ${RAG_CHECKS}/Phase 1/, ${RAG_CHECKS}/Phase 2/, etc.
RAG_CHECKS=/path/to/rag

# Cache Configuration
# Set to false to disable all caching (vector store, summaries, metadata)
# When disabled, all documents are processed fresh on every request (slower but always up-to-date)
# Useful for development/debugging or when content changes frequently
# Default: true (caching enabled for performance)
CACHE_ENABLED=true
